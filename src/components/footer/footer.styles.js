import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  footerContainer: {
    backgroundColor: "#fff",
    paddingTop: 5,
    paddingBottom: 5,
  },
  subText: {
    fontSize: 10,
    color: "#424242",
  },
});

export default styles;
